apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: globetrotter-data
  labels:
    app: globetrotter-backend
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
