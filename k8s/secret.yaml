apiVersion: v1
kind: Secret
metadata:
  name: globetrotter-secret
  labels:
    app: globetrotter-backend
type: Opaque
stringData:
  # Overrides the SECRET_KEY fallback in backend/app/utils/auth.py.
  # Replace before anything but local experimentation.
  GLOBETROTTER_SECRET: dev-secret-change-me
